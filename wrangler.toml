# Deploy aci-nve-proxy-v2 to extend the existing worker
# with SYKE and FMI routes.
#
# IMPORTANT: This extends the existing aci-nve-proxy worker.
# Deploy with: wrangler deploy
# The existing NVE route (/?week=current) remains unchanged.

name = "aci-nve-proxy"
main = "cloudflare-worker/aci-nve-proxy-v2.js"
compatibility_date = "2024-01-01"
